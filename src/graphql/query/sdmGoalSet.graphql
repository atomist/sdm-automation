query SdmGoalSet($owner: [String!], $repo: [String!], $providerId: [String!]){
  SdmGoal(_orderBy: "ts", _first: 1) {
    goalSetId
    goalSet
    repo(name: $repo, owner: $owner, providerId: $providerId) @required {
      name
      owner
      providerId
    }
  }
}